# Zenith / Nilus Lab — Phase 0 Master Inventory (`inventory.md`)

**Date:** 2026-09-22  
**Status:** PHASE 0 COMPLETE — STOPPED AT PHASE 0 GATE FOR FOUNDER REVIEW  
**Compliance:** All local usernames and machine-specific prefixes stripped (`<repo>/...` and `<artifacts>/...`). Every item is classified as **EXISTS**, **NOT FOUND**, or **UNCLEAR** with exact relative paths, byte sizes, modification dates, and line references.

---

## 0A — Prior Work That May Already Be Done (Items 1–6)

| # | Item | Status | Path, Size, Modified Date & Technical Description |
|---|---|---|---|
| **1** | Any prior audit, validation or claim-ledger document | **EXISTS** | **8 documents found** across repository and current audit workspace:<br>1. `<repo>/CATALOG_AUDIT_2026_01_24.md` (`4,767 bytes`, `2026-01-24`) — Semantic audit of `technical_catalog.html` Sections 1–6 (flagged 3.03B vs 3.06B params, "Residual FFN" vs "Transformer", and simulation target `0.1011`).<br>2. `<repo>/CATALOG_AUDIT_PHASE2_2026_01_24.md` (`7,359 bytes`, `2026-01-24`) — Phase 2 audit of Sections 10–27 (documented adding Simulation Disclaimer banner, DRP-Alpha-12 computational-only status).<br>3. `<repo>/TECHNICAL_GAPS_REPORT.md` (`12,418 bytes`, `2026-07-13`) — Prior internal gap report.<br>4. `<repo>/models/zenith_foundation_v1/validation_report.json` (`1,204 bytes`, `2026-07-13`) — Validation metrics for `zenith_foundation_v29` (`n_cells_validated=50,000`, `silhouette=-0.0398`, `batch_mixing=0.686`, `de_marker_results` 80–100%).<br>5. `<artifacts>/verification_results.md` (`2026-09-22`) — 10-Part Adversarial Verification of `run_zenith_screening_pipeline.py`.<br>6. `<artifacts>/asset_verification.md` (`2026-09-22`) — Part 1 forensic verification of `scvi_model_486k_real`, `celloracle_grn.csv`, and `adata.h5ad`.<br>7. `<artifacts>/zenith_full_master_report.md` (`2026-09-22`) — Part 2 real GRN perturbation screen report.<br>8. `<artifacts>/discovery_engine_live_audit_report.md` (`2026-09-22`) — Audit of live `/api/gpt-discovery/run` tournament outputs across 3 prompts. |
| **2** | Donor-level (pseudobulk, $n=14$) statistical analysis | **UNCLEAR** | **Partial donor-level work exists; full per-gene pseudobulk correlation table ($n=14$) not yet generated:**<br>• **Exists:** `<artifacts>/methods.md` & `<repo>/scripts/run_part2_real_screen.py` (`2026-09-22`) ran a **Leave-One-Donor-Out (14-fold LODO)** Ridge age clock across the 14 Litviňuková donors (`D1–D7, D11, H2–H7`, Test MAE = `6.85 years`) and `<repo>/train_real_age_clock.py` (`4,803 bytes`, `2026-05-18`) evaluated per-donor ElasticNet predictions across all 14 donors.<br>• **Exists (Neural subset):** `<repo>/models/neural_age_clock_v1/config.json` (`1,459 bytes`, `2026-07-14`) trained a 10-gene donor-level clock across `total_donors = 51` (`total_cells = 4,895`, `pearson_r = 0.248`, `mae_years = 14.63`).<br>• **Not Yet Done:** Per-donor pseudobulk mean correlation ($n=14$ values per gene vs donor age with 95% CI, $p$, and BH-$q$) for all 100 genes in `models/cell_type_genes.json` (`vCM` and `fibroblast` tables). Ready to run in Phase 4.1. |
| **3** | Dissociation / batch-confound analysis (`HSPB1`, `CRYAB` vs QC metrics) | **UNCLEAR** | **Flagged qualitatively in prior audit; quantitative cross-tabulation not yet executed:**<br>• `<artifacts>/discovery_engine_live_audit_report.md` (`2026-09-22`) identified that `HSPB1` ($r=-0.4182$, 5x higher in aged vCMs) and `CRYAB` ($r=-0.3522$, 2x higher) are canonical stress/chaperone markers and noted potential warm enzymatic dissociation / tissue-procurement confounding.<br>• **Not Yet Done:** Formal cross-tabulation of `adata.obs` donor age vs processing site (`Sanger` vs `Harvard`), suspension type (`cell` vs `nucleus`), sex, median UMI counts, `% mitochondrial`, and partial regression of `HSPB1`/`CRYAB` on site + sequencing depth. Ready to run in Phase 4.2. |
| **4** | Null-distribution or permutation analysis for the GRN screen | **UNCLEAR** | **Edge-permutation & 20-iteration edge-bootstrap exist; 1,000-random-4-TF null and degree-matched null not yet run:**<br>• **Exists:** `<artifacts>/control_results.md` (`2026-09-22`) ran a 100-iteration **GRN column-permutation (shuffled TF$\to$target) control**, proving all 8 cardiac knockdown controls collapse from `-1.8%..-76.4%` to `0.0000` (`SD <= 0.011`).<br>• **Exists:** `<artifacts>/uncertainty.md` (`2026-09-22`) ran a 20-iteration **80% GRN edge-subsampling bootstrap** across all 675 candidates (`NFKB1+MITF+CTCF+HIF1A` won `20/20` iterations, `95% CI = [+5.765%, +6.249%]`).<br>• **Not Yet Done:** Sampling 1,000 random 4-TF sets from the 238 qualifying TFs and 1,000 **degree-matched** 4-TF sets (`±10%` total out-degree of winner). Ready to run in Phase 4.4(a). |
| **5** | Degree / connectivity analysis of the GRN | **UNCLEAR** | **Per-candidate active edge counts exist; formal regression of score on $\log(\text{out-degree})$ across all 238 TFs not yet run:**<br>• **Exists:** `<artifacts>/screen_output.csv` (`675 rows`, `2026-09-22`) records `n_active_edges` for every single TF and multi-TF combination, and `<artifacts>/zenith_full_master_report.md` noted that `NFKB1+MITF+CTCF+HIF1A` is a high-out-degree hub cocktail (`748` active target edges in vCMs vs `350` for `NL-101`).<br>• **Not Yet Done:** Pearson/Spearman correlation across all 238 single TFs (`out-degree` vs `youth_restoration_pct`), $R^2$ of $\log(\text{degree})$ model, and residual re-ranking. Ready to run in Phase 4.4(b). |
| **6** | Any document mapping UI or website figures to their sources | **EXISTS** | **3 partial mapping documents exist:**<br>1. `<repo>/CATALOG_AUDIT_2026_01_24.md` & `<repo>/CATALOG_AUDIT_PHASE2_2026_01_24.md` map `technical_catalog.html` sections to `bridge_server.py`, `index.html`, and `js/script.js`.<br>2. `<artifacts>/discovery_engine_live_audit_report.md` maps the Tournament Discovery card, Arrhythmia Safety Gate, LNP Delivery Optimizer, and Clinical Trial Outcomes panels to `bridge_server.py` and `index.html`.<br>• A unified trace of every UI widget and public website metric is scheduled for Phase 1 (`website_claims.md`) and Phase 2.5 (`code_ui_fixes.md`). |

---

## 0B — Assets Claimed Publicly (Items 7–17)

| # | Item | Claim to Check Against | Status | Path, Size, Modified Date & Technical Findings |
|---|---|---|---|---|
| **7** | Script that generated `cell_type_genes.json` | — | **EXISTS** | **Found in repository root (2 scripts):**<br>1. `<repo>/extract_celltype_genes.py` (`9,544 bytes`, modified `2026-07-13`) — Loads `data/hca_full/heart_adult_full.h5ad` (`486,134` cells) + `models/scvi_model_486k_real`, splits 14 donors into `young <= 55.0y` (`D1, H2, H3, H5, H6, H7`) and `aged >= 65.0y` (`D4, D5, D6`), computes 20-D scVI latent centroids (`rejuv_vector = young_centroid - aged_centroid`), projects all cells of each cell type onto `rejuv_vector` (`projections = ct_latent @ rejuv_vector`), and calculates cell-level Pearson $r$ between gene expression and `projections`, saving top 50 positive and top 50 negative genes per cell type to `models/cell_type_genes.json`.<br>2. `<repo>/map_celltype_symbols.py` (`2,080 bytes`, modified `2026-07-13`) — Maps Ensembl IDs in `models/cell_type_genes.json` (`187,866 bytes`, modified `2026-07-25`) to HGNC symbols via `data/hca_full/ensembl_to_symbol_map.json`. |
| **8** | Any dataset with >100k cardiac cells | Site claims `486,134` and `2.42M` | **UNCLEAR** | **Trained model registries & UMAP embeddings for >100k and 1.96M cells exist locally; raw multi-GB `.h5ad` files were processed in Google Colab and not committed to git:**<br>• **Local `.h5ad` files:** `<repo>/models/scvi_model_hca/adata.h5ad` (`193,448,428 bytes`, `2026-01-07`) has `18,641` cells $\times$ `26,662` genes (`5,307` vCMs, `2,446` fibroblasts). `<repo>/models/zenith_foundation_v1/umap_latent.h5ad` (`13,708,303 bytes`, `2026-06-21`) contains latent UMAP coordinates.<br>• **Verified >100k / 1.96M Model Registries on disk:**<br>  - `<repo>/models/zenith_foundation_v1/model.pt` (`166,773,810 bytes`, `2026-07-13`) embeds `field_registries['X']['summary_stats'] = {'n_vars': 5858, 'n_cells': 1,962,128}` (`1.962M` cells across `14` batches, streamed via `<repo>/step1_fetch_cellxgene.py`).<br>  - `<repo>/models/scvi_model_486k_real/model.pt` (`14,242,461 bytes`, `2026-07-18`) embeds `n_cells = 99,993` (stratified 100k subsample of `heart_adult_full.h5ad` `486,134` cells via `<repo>/train_real_486k_scvi_rebuild_colab.py`). |
| **9** | Full Litviňuková atlas (~487k cells) | May already be downloaded | **NOT FOUND (Locally)** | The full `data/hca_full/heart_adult_full.h5ad` (`486,134` cells) is **not present on the local disk** (`data/hca_full/` is absent locally). As documented in `<repo>/train_real_486k_scvi_rebuild_colab.py:10-17` and `<repo>/extract_celltype_genes.py:21`, the `486,134`-cell `.h5ad` was mounted on **Google Colab** to train `scvi_model_486k_real` and export `models/cell_type_genes.json` (`total_cells: 486134`, `vCM: 125,289`, `fibroblast: 59,341`) and `models/cell_type_centroids.json`, while only the `18,641`-cell subset (`models/scvi_model_hca/adata.h5ad`, `193.4 MB`) was stored in the local repository. |
| **10** | Any second/larger trained model | Site claims `~500M` parameters | **EXISTS (37.2M params, not 500M)** | **Three larger trained scVI checkpoints exist in `models/`, though none has 500M parameters:**<br>1. `<repo>/models/zenith_foundation_v1/model.pt` (`166,773,810 bytes`, `2026-07-13`) — Trained for `150 epochs` on `1,962,128` cardiac cells (`n_vars=5,858`, `n_hidden=1024`, `n_latent=64`, `n_layers=4`). Exact parameter count: **`37,234,698` parameters (~37.2M)**.<br>2. `<repo>/models/neural_age_clock_v1/scvi_model.pt` (`166,774,258 bytes`, `2026-07-13`) — Same architecture (`37,234,698` parameters).<br>3. `<repo>/models/zenith_foundation_v29/model.pt` (`126,813,042 bytes`, `2026-07-01`) — `n_hidden=1024, n_latent=64, n_layers=4`, **`31,660,338` parameters (~31.7M)**.<br>*(Note: `<repo>/bridge_server.py:512-538` also defines an un-checkpointed `ZenithSDE` PyTorch class whose parameter math was calculated in `CATALOG_AUDIT_2026_01_24.md` as `3.06B` / `500M`, but no `.pt` weights exist for `ZenithSDE`).* |
| **11** | `celloracle_grn.csv` provenance | Previously found to be `np.corrcoef` top-5 | **EXISTS** | `<repo>/models/celloracle_grn.csv` (`4,781,568 bytes`, `2026-05-09`, `133,310` rows) was generated by `<repo>/grn_generator.py:10-31` (`1,105 bytes`, `2026-05-09`), which computes `np.corrcoef(X.T)` across all `26,662` genes in `adata.h5ad` and retains the top 5 highest Pearson correlations per gene (`26,662 × 5 = 133,310`). It is not a CellOracle motif-inferred GRN. *(In Part 2, `<repo>/models/omnipath_collectri_dorothea_human.tsv`, `18,502,368 bytes`, `74,302` literature TF-target edges, was downloaded to replace it).* |
| **12** | Benchmark or holdout evaluation outputs | Site claims `99.8% fidelity`, `98.2% causal fidelity`, `280% stability gain` | **UNCLEAR** | **Real validation JSONs exist with modest empirical metrics; the `99.8% / 98.2% / 280%` marketing figures trace to static manifest/UI literals:**<br>• **Real validation outputs:** `<repo>/models/zenith_foundation_v1/validation_report.json` (`1,204 bytes`, `2026-07-13`) reports `silhouette_cell_type = -0.0398`, `batch_mixing_score = 0.6860`, and marker recovery `80%–100%` (`92%` average in `ZENITH_FOUNDATION_MODEL_CARD.md:147`). `<repo>/models/age_clock.pkl` (`39,490 bytes`, `2026-07-13`) reports 5-fold CV MAE (`cv_mae_years`).<br>• **Static manifest:** `<repo>/REPRO_MANIFEST.json` (`1,115 bytes`, `2026-07-13`) contains hardcoded `"fidelity_score": 0.992`. No holdout evaluation script or log computes `99.8% fidelity`, `98.2% causal fidelity`, or `280% stability gain`. |
| **13** | Any wet-lab data of any kind | Site says `"three completed autonomous experiments"` | **NOT FOUND** | Zero wet-lab experimental data files (no FASTQ, BAM, plate-reader CSV, Western blot, microscopy TIFF, or cell culture logs) exist anywhere in the repository. `"Experiment_1.png"` and `"Experiment_2.png"` in `<repo>/` are UI screenshots of in-silico simulation runs. |
| **14** | Opentrons hardware or connection logs | Site claims OT-2/Flex integration, `88ms` deck latency | **NOT FOUND** | No physical Opentrons hardware connection, telemetry, USB/HTTP robot client, or `88ms` latency logs exist. `<repo>/bridge_server.py:5323` and `8050` (`/api/v2/robotics/opentrons`) are string-template generators that format Python text files (`zenith_opentrons_ot2_protocol.py`) for download. |
| **15** | Virtual trial engine outputs | Site claims `N=1,000` digital twins | **EXISTS (Synthetic Monte Carlo)** | `<repo>/services/virtual_trial_engine.py` (`9,551 bytes`, `2026-08-04`) and `<repo>/bridge_server.py:6202-6260` (`/run_virtual_trial`) exist and execute a fast NumPy parametric Monte Carlo loop (`np.random.normal` over synthetic patient covariates for `n_patients`, defaulting to `100` or `1,000`). It does not simulate 1,000 scVI single-cell transcriptomes. |
| **16** | scGPT integration — invoked at runtime? | UI shows a scGPT panel | **NOT FOUND (Label Only)** | `scGPT` is **never imported or invoked** anywhere in Python (`0` matches for `scgpt` across all `.py` files). In `<repo>/index.html:1680`, `"Multi-Omics Perturbation Predictor (scGPT)"` is a static HTML header above checkboxes (`Semaglutide`, `Omega-3`, `Plasmapheresis`, `Decitabine`, `Pitavastatin`) wired to a JavaScript lookup table in `index.html`. |
| **17** | `perturbation_engine.py` — called by anything in production? | — | **EXISTS (Called by API routes, NOT by GPT Discovery)** | `<repo>/perturbation_engine.py` (`28,508 bytes`, `2026-07-18`) is imported and instantiated in `<repo>/bridge_server.py:108-136` (`get_perturbation_engine()`) and is called by `/api/perturbation`, `/api/v1/perturbation` (`routers/api_v1.py`), and `bridge_server.py:7628`. However, the live **Tournament Discovery endpoint** (`/api/gpt-discovery/run`, `bridge_server.py:6651-6770`) does **not** call `PerturbationEngine.predict_factor_effect()`; it queries `models/cell_type_genes.json` + `gpt-4o`. |

---

## 0C — Live Code and UI Items (Items 18–23)

| # | Item | Status | Exact Code Location (`file:line`) & Behavior |
|---|---|---|---|
| **18** | `r = 0.999` rule at `bridge_server.py:6754–6758` — still present? | **EXISTS** | **Present in TWO places in `<repo>/bridge_server.py`:**<br>1. `bridge_server.py:6754–6758` (inside `/api/gpt-discovery/run` refinement prompt): `"CRITICAL: If the user prompt asks for specific external targets (like OCT4, SOX2, KLF4, SIRT1, SIRT6), you MUST include them in the final 8-gene panel with a correlation of 0.999..."`<br>2. `bridge_server.py:7423` (inside `system_base` prompt for `/api/gpt-discovery/stream` / secondary discovery route): `"If you include an external gene target (such as OCT4, SOX2, KLF4, SIRT1, SIRT5, SIRT6, NMN), set its correlation to 0.999..."` |
| **19** | OT-2 script export — still enabled? | **EXISTS (Enabled)** | **Live in UI and Backend:**<br>• UI button: `<repo>/index.html:6142` (`<button onclick="downloadOpentronsScript()">Export OT-2 Script</button>`) and JS handler at `index.html:6645–6692`.<br>• Backend endpoints: `<repo>/bridge_server.py:5323` (`/api/opentrons/generate`) and `bridge_server.py:8050` (`/api/v2/robotics/opentrons`). |
| **20** | Wet-Lab Dossier export — still enabled? | **EXISTS (Enabled)** | **Live in UI and Backend:**<br>• UI button: `<repo>/index.html:6143` (`<button onclick="printClinicalDossier()">Print Wet-Lab Dossier</button>`) and JS pop-up generator at `index.html:6551–6643` (`<title>Zenith Wet-Lab Validation Dossier</title>`).<br>• Backend endpoint: `<repo>/bridge_server.py:8097` (`/api/v2/dossier/generate`). |
| **21** | `"VERIFIED SAFE"` / `"approved for wet-lab validation"` strings — still emitted? | **EXISTS (Enabled)** | **Present in `<repo>/index.html` and `<repo>/services/neuros_substrate_service.py`:**<br>• `index.html:2160`: Static/default badge text `"VERIFIED SAFE — STABLE CONDUCTION (512-NODE CARDIAC SYNCYTIUM)"`.<br>• `index.html:6267` & `6612`: Emits `"VERIFIED SAFE"` and `"Cocktail approved for wet-lab validation"` when arrhythmia risk classification is safe.<br>• `services/neuros_substrate_service.py:422`: Returns `"safety_classification": "VERIFIED_SAFE"`. |
| **22** | Source of `DunedinPACE` / `DamAge` / `AdaptAge` values and the Harvard attribution | **EXISTS (Static JS Lookup Table)** | **Located in `<repo>/index.html:1736–2024` and `index.html:4560–4630`:**<br>• The values (`DunedinPACE: -0.09`, `Causal DamAge: -3.10 Yrs`, `Causal AdaptAge: +1.80 Yrs`) come from a **hardcoded JavaScript dictionary** in `index.html` triggered by checking UI intervention boxes (`chk-semaglutide`, `chk-omega3`, `chk-plasmapheresis`, `chk-decitabine`, `chk-pitavastatin`) or cocktail scoring heuristics.<br>• `index.html:2022` hardcodes the HTML string: `<span id="pred-clinical-citation">Source: Adiv A. Johnson & Harvard Genetic Studies, Frontiers in Genetics, 2026. Systematic review of 41 human aging clinical trials.</span>` |
| **23** | Where the `13.0`-year cap is implemented in code (catalogue `FIX 6`) | **EXISTS** | **Implemented in two places in `<repo>/bridge_server.py` and documented in `<repo>/technical_catalog.html`:**<br>1. `bridge_server.py:4697–4711`: `MAX_AGE_REDUCTION_YEARS = 13.0; age_reduction = min(raw_age, MAX_AGE_REDUCTION_YEARS)` (caps GPT-returned `age_reduction` in `/api/discover_hybrid`).<br>2. `bridge_server.py:6704`: `MAX_AGE_REDUCTION_YEARS = 13.0` (inside `/api/gpt-discovery/run`).<br>3. `technical_catalog.html:6673–6709` (`FIX 6`): Explicitly documents that `13.0 years` is a **hard upper cap** introduced so GPT-4o cannot return biologically impossible values like `"45 years"`, citing Sarkar et al. (2020) and Lu et al. (2020). |

---

## PHASE 0 GATE — STOPPED FOR FOUNDER REVIEW

Per the Master Remediation Plan v2 instructions:
> **Nothing is built or changed until Phase 0 is complete and the founder has reviewed it.**
> **Deliverable: `inventory.md`. Then STOP.**
