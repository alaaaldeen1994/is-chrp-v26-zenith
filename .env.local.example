# NEVER commit the real key.
# Create a fresh Boltz TEST key first, then a LIVE key only when ready.
BOLTZ_API_KEY=replace_with_rotated_key

# Live predictions above this estimated cost are blocked server-side.
# Set to 0 to disable this guard (not recommended).
NILUS_MAX_COST_USD=0.50
